//The function evenOrOdd such that it returns the string 'even' when the number paramegter is receives is even and 'odd' otherwise.
// Can you make it work with negative numbers too?

function evenOrOdd(number) {
  if (number % 2 === 0) {
    return 'even'
  }
  return 'odd'
}
